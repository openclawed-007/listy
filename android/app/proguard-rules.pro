# Firestore model classes are accessed via reflection-free manual mapping,
# so no extra keep rules are needed for them.

# Play services ships its own consumer rules; nothing extra needed for the
# Identity sign-in flow.
